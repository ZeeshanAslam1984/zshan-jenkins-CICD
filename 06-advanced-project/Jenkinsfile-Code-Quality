pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Setup') {
            steps {
                dir('06-advanced-project') {
                    // Use full path explicitly
                    sh '/var/lib/jenkins/.local/bin/poetry install --with dev'
                }
            }
        }

        stage('Test') {
            steps {
                dir('06-advanced-project') {
                    // Use full path explicitly for pytest
                    sh '/var/lib/jenkins/.local/bin/poetry run pytest'
                }
            }
        }
    }
}
