pipeline {
    agent any
    environment {
        PATH+EXTRA = "/var/lib/jenkins/.local/bin"
    }
    stages {
        stage('Setup') {
            steps {
                dir('06-advanced-project') {    // <- change directory here
                    sh 'poetry install --with dev'
                }
            }
        }

        stage('Test') {
            steps {
                dir('06-advanced-project') {    // <- change directory here
                    sh 'poetry run pytest'
                }
            }
        }
    }
}
